<%= render 'form' %>

<% if flash[:created] %>
  <br /><br />
  <h1 style="line-height:2em;">Tagged Link: <%= link_to @tagged_link.url, @tagged_link.url %></h1>
<% end %>

<% if !@links.empty? %>
  <br /><br />
  <div><h2>recently tagged links</h2></div>
  
  <% @links.each do |link| %>
    <div>
      <p><%= link_to link.url, link.url %>
      <%= link_to 'Destroy', link, confirm: 'Are you sure?', method: :delete %></p>
  <% end %>
<% end %>
